const { UUID } = require('sequelize');
const UserInfo = require('../../models/userinfo');

exports.postInfo = (req, res) => {
  { cummulativeTime, todaysTime, points, githubId, badge, uid}
}

exports.getInfo = (req, res) => {
  
}
